import React, { PropTypes } from 'react';

const LoadingSpinner = (props) => {
  return (<span style={{display:"inline"}}>....This is a loader....</span>);
};

LoadingSpinner.propTypes = {
};

export default LoadingSpinner;